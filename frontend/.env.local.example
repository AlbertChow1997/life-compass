# Copy this file to .env.local (gitignored) and fill in the real value.
# Vite only exposes env vars prefixed with VITE_ to client-side code.

# OAuth Web Client ID from Google Cloud Console (Credentials > OAuth 2.0 Client IDs).
# Same value the backend needs as GOOGLE_CLIENT_ID.
VITE_GOOGLE_CLIENT_ID=
