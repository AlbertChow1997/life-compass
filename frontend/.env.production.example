# Production frontend build settings for Vercel.
#
# frontend/vercel.json rewrites /api, /images and /uploads to the EC2 backend,
# so the browser can keep using same-origin HTTPS URLs and avoid mixed-content
# blocking while the EC2 backend is still HTTP-only.

VITE_API_BASE_URL=/api
VITE_ASSET_BASE_URL=
